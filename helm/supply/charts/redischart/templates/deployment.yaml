apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-{{ .Chart.Name }}-deployment
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Chart.Name }}
    environment: {{ .Values.environment }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Chart.Name }}
  template:
    metadata:
      labels:
        app: {{ .Chart.Name }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: {{ .Values.image }}
          ports:
            - containerPort: {{ .Values.redisPort }}
          env:
            - name: ALLOW_EMPTY_PASSWORD
              value: "yes"
          volumeMounts:
            - name: {{ .Release.Name }}-{{ .Chart.Name }}-data-pvc
              mountPath: /data
      volumes:
        - name: {{ .Release.Name }}-{{ .Chart.Name }}-data-pvc
          persistentVolumeClaim:
            claimName: {{ .Release.Name }}-{{ .Chart.Name }}-data-pvc
